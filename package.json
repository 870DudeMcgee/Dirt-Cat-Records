{
  "name": "dirt-cat-records",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test test/pricing.test.js test/paypal-api.test.js",
    "check:js": "node --check spells.js && node --check checkout.js && node --check success.js && node --check api/create-paypal-order.js && node --check api/capture-paypal-order.js && node --check api/checkout-config.js && node --check lib/checkout/pricing.js",
    "dev": "npx vercel dev"
  },
  "devDependencies": {}
}
